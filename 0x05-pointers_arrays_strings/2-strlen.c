#include "main.h"
/**
 * int _strlen - returns the length of a string
 * @s: pointer to the string to check
 * Return: void
 */
int _strlen(char *s)

{
	int longi = 0;

	while (s[i])
	{
	longi++;
	}
	return (longi);
}
